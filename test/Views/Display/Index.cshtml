@model IEnumerable<test.ViewModel.MultiTables>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>

            <input type="button" class="btn btn-info"
                   value="Edit"
                   onclick="location.href='@Url.Action("Edit", "Display",new { id = item.name  })'" />|
            <input type="button" class="btn btn-primary"
                   value="Details"
                   onclick="location.href='@Url.Action("Details", "Display",new { id = item.name  })'" />|
            <input type="button" class="btn btn-danger"
                   value="Delete"
                   onclick="location.href='@Url.Action("Delete", "Display",new { id = item.name  })'" />
            @*@Html.ActionLink("Edit", "Edit", new { id=item.name }) |
        @Html.ActionLink("Details", "Details", new { id=item.name }) |
        @Html.ActionLink("Delete", "Delete", new { id=item.name })*@
        </td>
    </tr>
}

</table>
