@model IEnumerable<test.Models.OrderProduct>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/layout1.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<style>
    #apply {
        font-family: "Trebuchet MS", Arial, Helvetica, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

        #apply td, #apply th {
            border: 1px solid #ddd;
            padding: 8px;
        }

        #apply tr {
            background-color: #f2f2f2;
        }

            #apply tr:hover {
                background-color: #ddd;
            }

        #apply th {
            padding-top: 12px;
            padding-bottom: 12px;
            text-align: left;
            background-color: #4CAF50;
            color: white;
        }
</style>
<table class="table" id="apply"style="color:black">
    <tr>
        <th>
      Order Id
        </th>
        <th>
            Date of Order
            @*@Html.DisplayNameFor(model => model.date_of_Order)*@
        </th>
        <th>
            Quantity
            @*@Html.DisplayNameFor(model => model.qunt)*@
        </th>
        <th>
            Total
            @*@Html.DisplayNameFor(model => model.total)*@
        </th>
        <th>
           Customer Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product1.ProductName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.orderId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_of_Order)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.qunt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.CustomerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product1.ProductName)
        </td>
        <td>
            <input type="button" class="btn btn-primary"
                   value="Edit"
                   onclick="location.href='@Url.Action("Edit","OrderProducts",new { id = item.orderId})'" />|
            <input type="button" class="btn btn-primary"
                   value="Details"
                   onclick="location.href='@Url.Action( "Details","OrderProducts", new { id = item.orderId})'" />|
            <input type="button" class="btn btn-danger"
                   value="Delete"
                   onclick="location.href='@Url.Action( "Delete","OrderProducts",new { id = item.orderId })'" />|
            @*@Html.ActionLink("Edit", "Edit", new { id = item.orderId }) |
            @Html.ActionLink("Details", "Details", new { id = item.orderId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.orderId })*@
        </td>
    </tr>
}

</table>
